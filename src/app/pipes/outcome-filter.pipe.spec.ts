import { OutcomeFilterPipe } from './outcome-filter.pipe';

describe('OutcomeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OutcomeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

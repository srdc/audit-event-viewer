import { InlineArrayPipe } from './inline-array.pipe';

describe('InlineArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new InlineArrayPipe();
    expect(pipe).toBeTruthy();
  });
});

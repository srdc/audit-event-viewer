import { AgentFilterPipe } from './agent-filter.pipe';

describe('AgentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AgentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
